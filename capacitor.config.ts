import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.criptomensajes.steam',
  appName: 'EncriptacionMensajes',
  webDir: 'dist'
};

export default config;
